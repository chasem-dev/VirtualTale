package dev.chasem.hg.virtualtale.emulator;

/**
 * Unified button enum covering both Game Boy and GBA inputs.
 * GB uses UP through SELECT. GBA adds L and R shoulder buttons.
 */
public enum EmulatorButton {
    UP, DOWN, LEFT, RIGHT, A, B, SELECT, START, L, R
}
